extends ./base

block content
  each post in posts
    article
      a(href='/post/'+post.slug, class='underline-hover-only')
        h1= post.title
      time(pubdate)= post.posted
      != marked(post.text)
